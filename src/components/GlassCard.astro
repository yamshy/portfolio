---
import type { GlassCardVariant, GlassCardSize } from '../types/index';

export interface Props {
	variant?: GlassCardVariant;
	size?: GlassCardSize;
	class?: string;
	hover?: boolean;
}

const { variant = 'default', size = 'medium', class: className = '', hover = false } = Astro.props;
---

<div class={className}>
	<slot />
</div>